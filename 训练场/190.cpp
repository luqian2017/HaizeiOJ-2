#include <stdio.h>
#include <stdlib.h>

#define  MAX_N 8000005

int prime[MAX_N];

void init() {
    for (int i = 2; i < MAX_N; i++) {
        for (int j = 2)
    }
}

int main() {
    
    return 0;
}
